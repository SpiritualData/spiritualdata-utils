from spiritualdata_utils import import_data

def import_nderf():
    """
    Import experiences from nderf.org.
    Only import those experiences that are not already in the database.

    Args:
        - 

    Returns:
        - 
    """
    for experience in experiences:
        # convert data model
        import_data()
